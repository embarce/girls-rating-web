# pnpm 11 官方推荐：显式放行需要 postinstall 的包
only-built-dependencies=esbuild,protobufjs

# 锁文件一致性（Docker / CI 必开）
frozen-lockfile=true

# 自动处理 peer deps（Vite / React 项目友好）
auto-install-peers=true
strict-peer-dependencies=false

# 提高 CI 稳定性
prefer-frozen-lockfile=true
